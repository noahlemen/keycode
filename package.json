{
  "name": "what-key-code",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "prettier": "0.0.7",
    "react-scripts": "0.8.5",
    "surge": "^0.18.0"
  },
  "dependencies": {
    "ismobilejs": "^0.4.0",
    "keycode": "^2.1.8",
    "react": "^15.4.2",
    "react-copy-to-clipboard": "^4.2.3",
    "react-dom": "^15.4.2",
    "react-motion": "^0.4.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "surge --project ./build --domain keycode.noahlemen.com",
    "pretty": "prettier --write src/*.js --single-quote"
  }
}
