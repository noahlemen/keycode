{
  "name": "what-key-code",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "prettier": "0.0.7",
    "react-scripts": "0.8.5",
    "surge": "^0.18.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-copy-to-clipboard": "^4.2.3",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "surge --project ./build --domain what-key-code.surge.sh",
    "pretty": "prettier --write src/*.js --single-quote"
  }
}
